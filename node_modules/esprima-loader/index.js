var esprima = require('esprima-fb');
var fs = require('fs');

module.exports = function(source){
  var ast = esprima.parse(source, { tolerant: true, loc: true, range: true }).body;
  fs.writeFileSync('./sources/esprima-ast-output.js', JSON.stringify(ast, null, '\t'));
  return source;
}
